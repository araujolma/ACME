- build in IAE a generic .mdl model with an “editable” sfunction: OK? Check!

- TODO: Test bildQuikModl without it being on the same folder as the target function.
	Watch out for functions like disp. They cause the argument to be treated as string even though there may be no single quotes whatsoever.

	PROBLEM with ‘return’ in functions… 

- PSBL: store not only variable “index” but also the occurrances (lines) of each. 

